type Aggregate{
    count : Int
}

extend type Query{
    aggregatePosting : Aggregate
}

input PaginationInput{
    page : Int = 0,
    size : Int = 10,
}

schema {
    query : Query,
    mutation: Mutation
}
